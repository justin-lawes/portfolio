---
layout: base.njk
activeNav: music
---
<main>
  <div class="project-grid">
    {%- for project in collections.projects %}
    {%- if "music" in project.data.pages %}
    <a href="{{ project.url }}" class="project-card">
      {% if project.data.thumbnail %}
      <img class="project-image square" src="{{ project.data.thumbnail }}" alt="{{ project.data.title }}">
      {% else %}
      <div class="project-image square"></div>
      {% endif %}
      <h3>{{ project.data.title }}</h3>
    </a>
    {%- endif %}
    {%- endfor %}
  </div>
</main>
